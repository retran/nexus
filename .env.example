# Copyright 2025 Andrew Vasilyev
# SPDX-License-Identifier: APACHE-2.0

# .env.example - Copy this to .env and customize your values. DO NOT COMMIT .env!

# === PostgreSQL Configuration ===
POSTGRES_USER=admin
POSTGRES_PASSWORD=CHANGE_ME_SUPER_SECRET_PASSWORD
POSTGRES_DB=nexus_db

# === Docker Compose ===
COMPOSE_PROJECT_NAME=nexus

# === Frontend Configuration ===
# Points to the REST API gateway through Traefik
VITE_API_URL=http://api.nexus.local

# === Google OAuth ===
# Get these from https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: http://api.nexus.local/api/auth/google/callback
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URL=http://api.nexus.local/api/auth/google/callback

# === JWT Configuration ===
# Change this to a strong random secret in production!
# Generate with: openssl rand -base64 32
JWT_SECRET=change-me-in-production-use-a-strong-secret

# === Frontend Configuration ===
FRONTEND_URL=http://nexus.local

# === Temporal Configuration ===
# These are set in docker-compose.dev.yaml, no need to change
# TEMPORAL_HOST=temporal:7233
# TEMPORAL_NAMESPACE=default
# TEMPORAL_TASK_QUEUE=nexus-task-queue
